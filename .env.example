# --- Database (Prisma + Postgres / Prisma Accelerate) ---
# Verplicht voor login en opslaan van analyses.
# Voor Prisma Accelerate: prisma+postgres://accelerate.prisma-data.net/?api_key=JOUW_API_KEY
# Voor directe Postgres: postgresql://user:password@host:5432/database
DATABASE_URL=

# JWT-geheim voor inlogsessies (kies een sterk geheim in productie)
JWT_SECRET=change-me-in-production

# Optioneel: basis-URL van de API (alleen nodig als de API op een ander domein draait dan de frontend).
# Laat leeg als dezelfde server zowel de frontend als /api/signup serveert (zoals server.js).
#
# Voorbeeld als de API ergens anders host:
# VITE_API_URL=https://api.aitrading.software

# MT5 file-based bridge: pad naar de map waar de EA status.json schrijft (FILE_COMMON = Terminal Common/Files/MT5_AI_Bot).
# Als balance niet laadt: zet MT5_BOT_PATH op exact het pad waar de EA schrijft (zie dashboard "Bridge: wat we ophalen" → Gelezen van).
# Op Mac met Wine vaak: ~/.wine/drive_c/Users/Public/Documents/MT5_AI_Bot of .../MetaQuotes/Terminal/Common/Files/MT5_AI_Bot
# MT5_BOT_PATH=/pad/naar/MT5_AI_Bot

# Symbool voor goud in MT5: veel brokers gebruiken "GOLD" i.p.v. "XAUUSD". Zet dan:
# MT5_SYMBOL_GOLD=GOLD

# Optioneel: MT5-accountgegevens voor dashboard. Bridge haalt login op via (1) EA status.json of (2) hier.
# Voor demo-account testen: zet je MT5 demo-login (bijv. 102199055).
# MT5_ACCOUNT=102199055
# MT5_SERVER=Ava-Real 1-MT5
# MT5_COMPANY=Ava Trade Ltd.
# MT5_MODE=Hedge
# Balance/equity/profit (voor KPI's) – zet demo/live waarden tot de EA ze in status.json schrijft
# MT5_BALANCE=10000
# MT5_EQUITY=10000
# MT5_PROFIT=0

# Overeenkomst ondertekenen: https://www.aitrading.software/agreement (geen token)
