# Optioneel: basis-URL van de API (alleen nodig als de API op een ander domein draait dan de frontend).
# Laat leeg als dezelfde server zowel de frontend als /api/signup serveert (zoals server.js).
#
# Voorbeeld als de API ergens anders host:
# VITE_API_URL=https://api.aitrading.software

# Samenwerkingsovereenkomst (alleen voor Vercel/API): geheim token voor de onderteken-link.
# De link die je deelt: https://www.aitrading.software/agreement/JOUW_TOKEN
# Genereer een lang, willekeurig token (bijv. 32+ tekens) en zet dat hier en in Vercel.
# AGREEMENT_TOKEN=een-geheim-token-minstens-32-karakters
